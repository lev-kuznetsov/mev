define(['d3'], function(d3){
	
	return function(){
		var self = this;
		
		//select svg
		self.DOM = d3.select('');
		
		//Function that pulls dataset info given dataset
		
		//Function that pulls dataset info, and sorts them for box plots
		self.drawBoxPlot = function(dataset){
			return
		}
	}
})