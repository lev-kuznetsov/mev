(function () {
    "use strict";

    define([], function () {

        return function (model, functions) {
            var self = this;

        	self.m = model;
            self.f = functions;
        };

    });

}());