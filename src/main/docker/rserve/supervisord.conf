[supervisord]
nodaemon=true
user=root
logfile=/var/log/supervisord.log
pidfile=/var/run/supervisord.pid
logfile_maxbytes=50MB
logfile_backups=10

[program:rserve]
command=R -e "Rserve::Rserve(debug=TRUE,args='--no-save')"
stdout_logfile = /var/log/rserve.log
